{
  "name": "mobilny-obhodchik",
  "version": "1.0.0",
  "private": true,
  "description": "Мобильный Обходчик — industrial equipment inspection system (hackathon MVP)",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "corepack pnpm -r --filter=./packages/* build",
    "dev": "corepack pnpm -r --parallel --filter=./packages/* dev",
    "typecheck": "corepack pnpm -r --filter=./packages/* typecheck",
    "test": "corepack pnpm -r --filter=./packages/* test",
    "lint": "corepack pnpm -r --filter=./packages/* lint",
    "dev:api": "corepack pnpm --filter @mobilny-obhodchik/api dev",
    "dev:web": "corepack pnpm --filter @mobilny-obhodchik/web dev",
    "dev:mobile": "corepack pnpm --filter @mobilny-obhodchik/mobile start",
    "db:migrate": "corepack pnpm --filter @mobilny-obhodchik/api db:migrate",
    "db:seed": "corepack pnpm --filter @mobilny-obhodchik/api db:seed",
    "db:reset": "corepack pnpm --filter @mobilny-obhodchik/api db:reset",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down -v"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "prettier": "^3.2.5"
  }
}
